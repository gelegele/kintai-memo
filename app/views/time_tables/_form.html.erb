<%= form_for(@time_table) do |f| %>
  <% if @time_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_table.errors.count, "error") %> prohibited this time_table from being saved:</h2>

      <ul>
      <% @time_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'start' %><br />
    <div>
      <%= f.select :fixed_start_hours, hours_list, {}, class: 'input-small' %>:
      <%= f.select :fixed_start_minutes, minutes_list, {}, class: 'input-small' %>
    </div>
  </div>
  <div class="field">
    <%= f.label 'end' %><br />
    <div>
      <%= f.select :fixed_end_hours, hours_list, {}, class: 'input-small' %>:
      <%= f.select :fixed_end_minutes, minutes_list, {}, class: 'input-small' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
